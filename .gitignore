# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
# Note: .terraform.lock.hcl is intentionally NOT ignored — it should be
# committed so all developers use the same provider versions.

# GCP credentials — never commit these
*.json
!package.json
!package-lock.json

# Jekyll (legacy)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
/vendor
Gemfile.lock

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
